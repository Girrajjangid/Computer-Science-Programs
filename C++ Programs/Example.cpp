#include<iostream>
using namespace std;
#include<conio.h>
#include<stdio.h> 

int main(){
double a,b;
char ch;   // if we wants to get only 1 character like in switches then this use 
z:
ch=cin.get();
   cout<<ch<<endl;
cin.get(ch);  // also we write like this
   cout<<ch<<endl;
ch=cin.get(); 
   cout<<ch;
char q='A';
//int i=90;
//gets(i);
goto z;
return 0;}
